# ====================================
# Environment Configuration
# สำหรับ Docker Deployment บน VM
# ====================================
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามต้องการ:
# cp env.example .env

# ====================================
# Database Configuration
# ====================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeThisPassword123!
POSTGRES_DB=ticketing_system

DB_HOST=database
DB_PORT=5432
DB_NAME=ticketing_system
DB_USER=postgres
DB_PASSWORD=ChangeThisPassword123!

# ====================================
# JWT Configuration
# ====================================
# ⚠️ สำคัญ: เปลี่ยนเป็น secret key ที่ยาวและปลอดภัย
JWT_SECRET=change-this-to-your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# ====================================
# Application Configuration
# ====================================
NODE_ENV=production
PORT=3000

# ====================================
# Next.js Configuration
# ====================================
# เปลี่ยน localhost เป็น IP หรือ Domain ของ VM
# ตัวอย่าง: http://192.168.1.100:3000 หรือ https://yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# ====================================
# หมายเหตุสำหรับการใช้งาน:
# ====================================
# 1. คัดลอกไฟล์นี้: cp env.example .env
# 2. เปลี่ยน POSTGRES_PASSWORD และ DB_PASSWORD ให้เหมือนกัน
# 3. เปลี่ยน JWT_SECRET เป็นค่าที่ปลอดภัย (ยาวอย่างน้อย 32 ตัวอักษร)
# 4. เปลี่ยน NEXT_PUBLIC_API_URL ให้ตรงกับ IP หรือ domain ของ VM
# 5. ไฟล์ .env จะไม่ถูก commit เข้า git (ปลอดภัย)

